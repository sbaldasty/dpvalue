% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize.R
\name{noisy_credible_interval}
\alias{noisy_credible_interval}
\title{Credible interval for a scalar noisy value}
\usage{
noisy_credible_interval(x, p = 0.95, n = 1000, rng = NULL)
}
\arguments{
\item{x}{A single noisy value -- e.g. one element of a noisy column
(however it was selected down to one row), or the result of
\code{sum()}/\code{mean()}/arithmetic on one.}

\item{p}{Central probability mass to cover (default 0.95).}

\item{n}{Number of joint posterior draws.}

\item{rng}{Optional integer seed, for reproducibility.}
}
\value{
A length-2 numeric vector \code{c(lower, upper)}.
}
\description{
Draws \code{n} joint posterior samples of \code{x} (via the wrapped Python
\code{NoisyValue.credible_interval}) and returns the central \code{p} interval. If
\code{x} was built out of other noisy values via arithmetic (e.g. a sum or
difference), the samples correctly reflect any shared latent/noise
dependencies baked into its posterior.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/census.R
\name{get_dhc_variables}
\alias{get_dhc_variables}
\title{Read named DHC variables as a tidy long table, tidycensus-style}
\usage{
get_dhc_variables(
  geography,
  variables,
  state = NULL,
  county = NULL,
  root = "data/2020-dhc-nmf-parquets",
  nonnegative = TRUE,
  apply_constraints = TRUE
)
}
\arguments{
\item{geography}{One of \code{"us"}, \code{"state"}, or \code{"county"} (\code{"county"}
requires \code{state}).}

\item{variables}{A named list. Each element is itself a list with a
\code{query} entry (a query name from Python's \code{dhc_queries()}, e.g.
\code{"sex*hispanic"}) plus zero or more \code{axis = "level"} filters (e.g.
\code{sex = "male"}) narrowing which cells of that query get summed into
the variable. Axes of the query not named in the filter are summed
over entirely.}

\item{state, county, root, nonnegative, apply_constraints}{Passed through to
\code{\link[=get_dhc]{get_dhc()}}.}
}
\value{
A tibble with columns \code{geoid}, \code{geocode}, \code{aian}, \code{variable}, and
a noisy \code{value} column -- one row per geography per requested
variable, the same shape \code{tidycensus::get_decennial()} returns.
}
\description{
A convenience layer over \code{\link[=get_dhc]{get_dhc()}} for callers coming from
\code{tidycensus::get_decennial(variables = c(name = "P12_002N", ...))}. The
NMF has no equivalent catalog of individually pre-tabulated variable
codes -- differential-privacy noise was measured against cross-tabulation
\emph{query workloads}, not a fixed set of published cells -- so there is no
Python-layer analog of a \code{"P12_002N"}-style variable name to look up.
Each \code{variables} entry here instead names a query plus the axis label(s)
that pick out its cell(s); axes left unspecified are summed over. That
sum is exactly the same linear composition on the underlying posterior
that a \code{dplyr::filter() |> summarise(sum(value))} pipeline over
\code{\link[=get_dhc]{get_dhc()}}'s output would perform -- this just gives it a friendly name
and tidycensus's one-row-per-geography-per-variable shape, so the two
fetches read the same at the call site even though the DHC one is
visibly reducing real query cells rather than looking up a variable that
was already published.
}
\examples{
\dontrun{
get_dhc_variables(
  "county", state = "VT", county = "007",
  variables = list(
    male = list(query = "sex*hispanic", sex = "male"),
    female = list(query = "sex*hispanic", sex = "female")))
}
}

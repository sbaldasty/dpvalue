% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/census.R
\name{get_decennial}
\alias{get_decennial}
\title{Read named DHC variables as a tidy long table, tidycensus-style}
\usage{
get_decennial(
  geography,
  variables,
  year = 2020,
  sumfile = "dhc",
  state = NULL,
  county = NULL,
  geometry = FALSE,
  root = "data/2020-dhc-nmf-parquets",
  nonnegative = TRUE,
  apply_constraints = TRUE
)
}
\arguments{
\item{geography}{One of \code{"us"}, \code{"state"}, or \code{"county"} (\code{"county"}
requires \code{state}).}

\item{variables}{A named character vector of literal Census variable
codes, exactly as passed to \code{tidycensus::get_decennial()}, e.g.
\code{c(male = "P12_002N", female = "P12_026N")}.}

\item{year}{Must be \code{2020} -- the only year this package has DHC data
for. Accepted for call-shape parity with \code{tidycensus::get_decennial()}.}

\item{sumfile}{Must be \code{"dhc"}; other \code{tidycensus} sumfiles (e.g. \code{"pl"})
aren't wrapped by this function yet.}

\item{state, county, root, nonnegative, apply_constraints}{Passed through to
\code{\link[=get_dhc]{get_dhc()}}.}

\item{geometry}{Must be \code{FALSE}; this package doesn't return \code{sf}
geometries. Accepted for call-shape parity with
\code{tidycensus::get_decennial()}.}
}
\value{
A tibble with columns \code{GEOID}, \code{geocode}, \code{aian}, \code{variable}, and
a noisy \code{value} column -- one row per geography per requested
variable, the same shape \code{tidycensus::get_decennial()} returns (plus
\code{geocode}/\code{aian}, which carry DP-relevant provenance tidycensus has no
analog for).
}
\description{
A drop-in replacement for
\code{tidycensus::get_decennial(variables = c(name = "P12_002N", ...))} when
\code{sumfile = "dhc"}: \code{variables} takes the same literal Census variable
codes tidycensus does. Each code is resolved via the Python
\code{census.dhc_table_variables()} crosswalk into the \code{get_dhc()} query and
axis filter(s) that reconstruct that published cell from the actual NMF
measurements -- see its docstring for why that reconstruction is
necessary (the NMF was measured against query workloads, not published
table cells) and how it picks the lowest-variance query available for
each cell.
}
\details{
Only table P12 ("Sex by Age") is covered so far, and only 43 of its 49
codes: codes from other tables (\code{"P1_..."}, \code{"H1_..."}, etc.) error
clearly, as do 6 of P12's own codes -- Male/Female 20 years, 21 years,
and 22 to 24 years. Those six have no reliable NMF reconstruction: the
only query with single-year age resolution is \code{"detailed"}, and summing
the ~5,000 detailed cells one of these buckets marginalizes over
produces a posterior far from its own observed value (most of those
cells fall in PL94 constraint blocks with 3+ still-free cells, where the
block total is only an upper bound rather than a tight joint
constraint -- see Python's \code{census.dhc_table_variables()} docstring).
Every other P12 code checks out: its reconstruction's credible interval
sits tightly around its own observed value, as expected.

Unlike \code{tidycensus::get_decennial()}, the returned tibble has no \code{NAME}
column: that requires a live FIPS-to-name geography lookup this package
doesn't have offline. Filter on \code{GEOID} instead.
}
\examples{
\dontrun{
get_decennial(
  "county", state = "VT", county = "007", year = 2020, sumfile = "dhc",
  variables = c(male = "P12_002N", female = "P12_026N"))
}
}
